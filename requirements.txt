aiohttp>=3.14.1
Pillow>=12.2.0
# Required native Rust core (mine_sentinel_rs). Install the platform wheel
# produced by the "Build Rust wheels" GitHub Actions workflow before loading
# this branch; the pure-Python fallback has intentionally been removed.
